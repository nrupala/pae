{
  "name": "pae-ui",
  "version": "0.1.0",
  "description": "PAE Dashboard - Vanilla TypeScript + Web Components",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch & live-server dist/",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4",
    "live-server": "^1.2"
  }
}
